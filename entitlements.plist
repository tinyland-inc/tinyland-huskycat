<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE plist PUBLIC "-//Apple//DTD PLIST 1.0//EN" "http://www.apple.com/DTDs/PropertyList-1.0.dtd">
<plist version="1.0">
<dict>
    <!-- Allow executing binaries and scripts -->
    <key>com.apple.security.cs.allow-unsigned-executable-memory</key>
    <true/>
    
    <!-- Allow loading of arbitrary libraries -->
    <key>com.apple.security.cs.disable-library-validation</key>
    <true/>
    
    <!-- Allow network access for downloading schemas and CI tools -->
    <key>com.apple.security.network.client</key>
    <true/>
    
    <!-- Allow file system access for validation tools -->
    <key>com.apple.security.files.user-selected.read-write</key>
    <true/>
    
    <!-- Allow running development tools like git, podman, etc. -->
    <key>com.apple.security.cs.allow-jit</key>
    <true/>
    
    <!-- Required for PyInstaller binaries -->
    <key>com.apple.security.cs.disable-executable-page-protection</key>
    <true/>
    
    <!-- Allow runtime code execution (needed for Python bytecode) -->
    <key>com.apple.security.cs.allow-dyld-environment-variables</key>
    <true/>
</dict>
</plist>